'use strict';

describe('myApp.deals module', function() {

  beforeEach(module('myApp.deals'));

  describe('deals controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('DealsCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});